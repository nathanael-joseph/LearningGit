def merge_sort(lst):
    #split list into left and right.
    
    # sorted_left = merge_sort(left)
    # sorted_right = merge_sort(right)

    # osrted_list = merge(sorted_left, sorted_right)

    # return sorted_list
